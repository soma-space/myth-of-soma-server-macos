# Use a strong password for the SQL Server administrator account.
MSSQL_SA_PASSWORD=ChangeThis-Soma-SA-Password-42!
MSSQL_PORT=1433

# Keep these on loopback until local client login is verified.
SOMA_BIND_IP=127.0.0.1
SOMA_ADVERTISED_IP=127.0.0.1
